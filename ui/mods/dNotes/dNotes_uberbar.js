(function() {
	
	// TODO Integrate notes in Uberbar.
/*
	model.dNotes_notes = ko.observableArray([]);

	if(localStorage.dNotes_notes)
	{
		var notes = decode(localStorage.dNotes_notes);
		for(var player in notes)
		{
			if(dNotes.hasNote(player))
				model.dNotes_notes.push(player);
		}
	}

	$('#contacts-tabs').append(
		'<li>' +
		'  <a href="#notes-pane" data-toggle="tab" data-bind="click_sound: \'default\', rollover_sound: \'default\'">Notes</a>' +
		'</li>'
	);

	$('#contacts-pane').after(
		'<div class="tab-pane tabs-contacts" id="notes-pane">' +
		'  <div class="div-contact-category">' +
		'  <!-- ko foreach: dNotes_playerswithnotes -->' +
		'  <div data-bind="template: { name: \'contact-template\', data: $data }"></div>' +
		'  <!-- /ko -->' +
		'</div>'
	);


	var templatetext=$(jQuery.parseHTML($('#contact-template').text())[1]);
	templatetext.children('.status-username').before('<div>hi</div>');
	$('#contact-template').text(templatetext.html());*/

})();
